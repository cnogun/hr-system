<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>시스템 공지사항 작성 - 인사관리 시스템</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
  <style>
    body { 
      background: linear-gradient(135deg, #f8fafc 0%, #e2eafc 100%);
      min-height: 100vh;
    }
    
    .notice-form {
      background: white;
      border-radius: 15px;
      box-shadow: 0 8px 25px rgba(0,0,0,0.1);
      padding: 2rem;
    }
    
    .form-control:focus {
      border-color: #667eea;
      box-shadow: 0 0 0 0.2rem rgba(102, 126, 234, 0.25);
    }
    
    .btn-primary {
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      border: none;
      padding: 0.75rem 2rem;
      font-weight: 600;
    }
    
    .btn-primary:hover {
      transform: translateY(-2px);
      box-shadow: 0 8px 25px rgba(102, 126, 234, 0.3);
    }
  </style>
</head>
<body>
  <%- include('header') %>
  
  <div class="container mt-4">
    <div class="row justify-content-center">
      <div class="col-lg-8">
        <div class="notice-form">
          <div class="text-center mb-4">
            <h2 class="fw-bold text-primary">
              <i class="fa-solid fa-bullhorn me-2"></i>
              시스템 공지사항 작성
            </h2>
            <p class="text-muted">중요한 공지사항을 작성하여 직원들에게 전달하세요.</p>
          </div>
          
          <form id="noticeForm">
            <div class="mb-3">
              <label for="title" class="form-label fw-semibold">
                <i class="fa-solid fa-heading me-2"></i>제목
              </label>
              <input type="text" class="form-control form-control-lg" id="title" name="title" 
                     placeholder="공지사항 제목을 입력하세요" required>
            </div>
            
            <div class="mb-3">
              <label for="category" class="form-label fw-semibold">
                <i class="fa-solid fa-tags me-2"></i>카테고리
              </label>
              <select class="form-select form-select-lg" id="category" name="category" required>
                <option value="">카테고리를 선택하세요</option>
                <option value="일반">일반</option>
                <option value="중요">중요</option>
                <option value="긴급">긴급</option>
                <option value="업데이트">시스템 업데이트</option>
                <option value="점검">시스템 점검</option>
              </select>
            </div>
            
            <div class="mb-3">
              <label for="content" class="form-label fw-semibold">
                <i class="fa-solid fa-file-text me-2"></i>내용
              </label>
              <textarea class="form-control" id="content" name="content" rows="10" 
                        placeholder="공지사항 내용을 상세히 입력하세요" required></textarea>
            </div>
            
            <div class="mb-3">
              <label for="priority" class="form-label fw-semibold">
                <i class="fa-solid fa-flag me-2"></i>우선순위
              </label>
              <select class="form-select" id="priority" name="priority" required>
                <option value="낮음">낮음</option>
                <option value="보통" selected>보통</option>
                <option value="높음">높음</option>
                <option value="긴급">긴급</option>
              </select>
            </div>
            
            <div class="mb-4">
              <label for="expiryDate" class="form-label fw-semibold">
                <i class="fa-solid fa-calendar-times me-2"></i>만료일
              </label>
              <input type="date" class="form-control" id="expiryDate" name="expiryDate" 
                     min="<%= new Date().toISOString().split('T')[0] %>">
              <div class="form-text">만료일을 설정하지 않으면 영구적으로 표시됩니다.</div>
            </div>
            
            <div class="d-grid gap-2 d-md-flex justify-content-md-end">
              <button type="button" class="btn btn-outline-secondary btn-lg me-md-2" onclick="history.back()">
                <i class="fa-solid fa-arrow-left me-2"></i>취소
              </button>
              <button type="submit" class="btn btn-primary btn-lg">
                <i class="fa-solid fa-paper-plane me-2"></i>공지사항 등록
              </button>
            </div>
          </form>
        </div>
      </div>
    </div>
  </div>
  
  <%- include('footer') %>
  
  <script>
    document.getElementById('noticeForm').addEventListener('submit', async function(e) {
      e.preventDefault();
      
      const formData = new FormData(this);
      const noticeData = {
        title: formData.get('title'),
        category: formData.get('category'),
        content: formData.get('content'),
        priority: formData.get('priority'),
        expiryDate: formData.get('expiryDate') || null
      };
      
      try {
        const response = await fetch('/notice', {
          method: 'POST',
          headers: {
            'Content-Type': 'application/json',
          },
          body: JSON.stringify(noticeData)
        });
        
        if (response.ok) {
          alert('공지사항이 성공적으로 등록되었습니다.');
          window.location.href = '/notice';
        } else {
          const errorData = await response.json();
          alert('공지사항 등록에 실패했습니다: ' + (errorData.message || '알 수 없는 오류'));
        }
      } catch (error) {
        console.error('공지사항 등록 오류:', error);
        alert('공지사항 등록 중 오류가 발생했습니다.');
      }
    });
    
    // 카테고리에 따른 우선순위 자동 설정
    document.getElementById('category').addEventListener('change', function() {
      const category = this.value;
      const prioritySelect = document.getElementById('priority');
      
      if (category === '긴급') {
        prioritySelect.value = '긴급';
      } else if (category === '중요') {
        prioritySelect.value = '높음';
      } else if (category === '일반') {
        prioritySelect.value = '보통';
      }
    });
  </script>
</body>
</html>
