<%- include('header') %>
<div class="container py-5">
  <div class="row justify-content-center">
    <div class="col-md-6">
      <div class="card shadow">
        <div class="card-body text-center">
          <h4 class="mb-4 text-danger">정말로 이 직원을 삭제하시겠습니까?</h4>
          <div class="mb-3">
            <b>이름:</b> <%= employee.name %><br>
            <b>부서:</b> <%= employee.department || '-' %><br>
            <b>이메일:</b> <%= employee.email %>
          </div>
          <form action="/admin/employees/<%= employee._id %>/delete" method="POST" class="d-inline">
            <button type="submit" class="btn btn-danger px-4">삭제</button>
          </form>
          <a href="/admin/employees/<%= employee._id %>" class="btn btn-secondary px-4 ms-2">취소</a>
        </div>
      </div>
    </div>
  </div>
</div>
<%- include('footer') %> 